@model IEnumerable<QuickCode.Models.DowntimeType>

@{
    ViewBag.Title = "Downtime Types";
}
<style>
    body {
        background: url("http://i.imgur.com/2JbMKmd.jpg") no-repeat center center fixed;
        -webkit-background-size: cover;
        -moz-background-size: cover;
        -o-background-size: cover;
        background-size: cover;
    }
</style>
@if (Request.IsAuthenticated)
{
@*<h2>Downtime Type Information</h2>
<p>Each type of common Downtime type is listed here</p>

<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
    <div class="container">
        <div class="row" style="margin-bottom: 10px;">
            <div class="col-lg-12">
                <h2>Downtime Types</h2>
                <p>Create a New Record</p>
                <a href="@Url.Action("Create", "DowntimeTypes")" class="btn btn-primary btn-lg btn-embossed">
                    Add
                    <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span>
                </a>
            </div>
        </div>


        <div class="row">
            <div class="col-lg-12">

                <table class="table table-bordered table-responsive table-striped">
                    <thead style="background-color:#16a085; color:#ffffff;">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Name)
                            </th>
                            <th>Options</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td style="width: 250px;">
                                    @Html.ActionLink("Edit", "Edit", new { id = item.DowntimeTypeID }) |
                                    @Html.ActionLink("Details", "Details", new { id = item.DowntimeTypeID }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.DowntimeTypeID })
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
                </div>
            </div>
            </div>
            }
